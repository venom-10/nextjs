import { Poppins, Lusitana } from "next/font/google";

export const lusit = Lusitana({ weight: "700", subsets: ["latin"] });
export const poppins = Poppins({ subsets: ["devanagari"], weight: "400" });
